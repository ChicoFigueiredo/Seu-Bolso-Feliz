{
  "name": "@sbf/operations",
  "version": "0.0.1",
  "private": true,
  "description": "Serviços de domínio compartilhados: hash, fingerprint, idempotência",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sbf/domain": "workspace:*",
    "@sbf/ingestion-types": "workspace:*",
    "@sbf/validation": "workspace:*",
    "@supabase/supabase-js": "^2"
  }
}
